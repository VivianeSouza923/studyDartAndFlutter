void main(List<String> args) {
  var lista = [1, 2, 3, 4, 5];

  print(lista);

  // tamanho da lista
  print(lista.length);
  // bota a lista ao contrário
  print(lista.reversed);
  // para obter o código de hash da lista
  print(lista.hashCode);
}
